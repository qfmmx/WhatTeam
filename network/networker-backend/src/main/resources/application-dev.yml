# 本地开发环境配置（dev profile）—— 不同步到 git
server:
  port: 8085   # 8081 被本机其它项目(Kbox)占用，改用 8085
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nepu?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&allowZeroDateTime=true&zeroDateTimeBehavior=convertToNull
    username: root
    password: root   # 本地 MySQL(Docker) root 密码
