# ============================================================
# 本地开发环境配置（dev profile）
# 通过 application.yml 中 spring.profiles.active=dev 激活
# 仅覆盖本地数据库连接，其余配置继承 application.yml
# ============================================================
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nepu_environment?useUnicode=true&characterEncoding=utf-8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: root
    password: root   # 本地 MySQL(Docker) root 密码
