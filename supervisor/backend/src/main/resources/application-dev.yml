# 本地开发环境配置（dev profile）—— 不同步到 git
server:
  port: 8082
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nep?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: root   # 本地 MySQL(Docker) root 密码
